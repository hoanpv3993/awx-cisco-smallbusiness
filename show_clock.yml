---
vars:
  cli:
    host: all
    username: cisco
    password: Abc123456
    transport: cli
   tasks:
   - name: run show version on remote devices
     nxos_command:
      commands: show version
      provider: "{{ cli }}"
