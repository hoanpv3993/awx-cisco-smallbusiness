---

- name: wipe out demo configs
  hosts: all
  connection: local
  gather_facts: no

  tasks:
  - name: Command Uptime
    ios_command:
      commands:
        - show run
    register: system
  - debug: msg="{{ system.stdout }}"
