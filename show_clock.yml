---
- name: Show clock Cisco...!!!
  hosts: all
  gather_facts: false
  connection: local
  vars:
   cli:
    username: cisco
    password: ABc123456
    transport: cli
  tasks:
   - name: show version
     ios_command:
      commands: show run
     register: output
